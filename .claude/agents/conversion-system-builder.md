---
name: conversion-system-builder
description: Use this agent to build the conversion mechanics (funnel steps, calls to action, follow-up sequences) once content angles are defined. STUB — not yet configured with real instructions; do not invoke for production work until this is filled in.
tools: Read, Grep, Glob, Write, Edit, WebSearch, WebFetch
model: inherit
---

# PLACEHOLDER — NOT YET WRITTEN

This is a skeleton stub created while setting up the four-subagent system. It is not ready to
use. Fill in the sections below before relying on this agent.

## Role
TBD — describe exactly how this agent should turn content angles into a conversion system.

## Inputs
TBD — the file path to `content-angle-strategist`'s output goes here once defined.

## Output
TBD — what file(s) does this agent write to `outputs/`, and in what format?

## Handoff
TBD — this is the last agent in the chain for now. Note any downstream file paths here once
a fifth stage or orchestrator exists.
